[project]
name = "jpbizday"
version = "0.0.3"
description = "A library for obtaining Japanese business days."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "jpholiday>=1.0.3",
]

[dependency-groups]
dev = [
    "mypy>=1.19.1",
    "pyinstaller>=6.19.0",
    "pytest>=9.0.2",
    "ruff>=0.15.2",
]
